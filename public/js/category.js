// const selectCategory = async (event) => {
//   event.preventDefault();

//   const category = document.querySelector(".side bar name").id; //add in sidebar name

//   document.location.replace(`/category/${category}`);
// };

// document
//   .querySelector(".side bar name")
//   .addEventListener("click", selectCategory);
